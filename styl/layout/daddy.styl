/**
 * Daddy
 * Primarily used by desktop computers.
 * Should be configured second _after_ baby.
 */
@media daddy
	#container
		padding (_goldilocks_padding*2)em
		margin 0 auto
		max-width (_goldilocks*2)em
		min-height (lineHeight*12)

// @media daddyOnly
