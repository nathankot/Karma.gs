/**
 * Core _layouting_ styles go here.
 * Typography, headings, background etc.
 * These styles should be implemented on ALL device sizes.
 *
 * Individual components and modules must go into SEPARATE files.
 */

/*
Scroll bar settings
====================================== */
html
	overflow-y scroll
	&
	body
		overflow-x hidden

/*
Generic elements
====================================== */
body
	background-color offWhite

h1,
h2,
h3,
h4
	margin-bottom (lineHeight/2)em

	hgroup &
		margin-bottom 0
		margin-left .1em
		&:first-child
			margin-left 0

hgroup
	margin-bottom (lineHeight/2)em

p,
ul,
ol,
dl,
blockquote
	margin-bottom lineHeight
	max-width _goldilocks

a,
a:visited
	outline none

a:hover
	outline none

a:active,
a:focus
	outline none

header
	margin-bottom lineHeight
section
	margin-bottom lineHeight


/*
MODULES
Modules tend to be app-specific
They are blocks of content/design that can be modulized
====================================== */
/* Import your modules here */
